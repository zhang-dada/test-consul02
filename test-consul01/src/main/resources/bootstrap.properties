spring.application.name=server001


#多环境配置的分隔符
spring.cloud.consul.config.profile-separator=,
spring.profiles.active=dev

#存储配置的最顶层前缀目录，默认的是config
spring.cloud.consul.config.prefix=config
#配置的存储格式
spring.cloud.consul.config.format=properties
#配置consul配置中心的应用名
spring.cloud.consul.config.default-context=${spring.application.name}

#存储配置目录（用来最终的存储配置信息）
spring.cloud.consul.config.data-key=my-data.key

spring.cloud.consul.config.watch.delay=2000

spring.cloud.consul.config.watch.enabled=true